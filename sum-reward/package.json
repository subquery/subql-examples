{
  "name": "sum-reward",
  "version": "0.1.0",
  "description": "This subquery indexes bond, reward and slash from events of finalized block",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "prepack": "rm -rf dist && npm build",
    "test": "jest",
    "codegen": "./node_modules/.bin/subql codegen"
  },
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Jay Ji",
  "license": "Apache-2.0",
  "devDependencies": {
    "@polkadot/api": "^3.4.2-17",
    "@subql/types": "^0.4.0",
    "typescript": "^4.1.3",
    "@subql/cli": "^0.5.0"
  }
}
